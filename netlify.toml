[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "20.18.0"

[functions]
  # Use esbuild to bundle functions and keep supabase client external
  node_bundler = "esbuild"
  external_node_modules = ["@supabase/supabase-js"]
